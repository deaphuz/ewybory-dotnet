@model IEnumerable<ewybory_dotnet.Models.VoterGroup>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Voter)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Group)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Voter.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Group.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VoterGroupId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VoterGroupId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VoterGroupId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
