@model IEnumerable<ewybory_dotnet.Models.GroupElection>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Election)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Group)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Election.Surname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Group.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GroupElectionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GroupElectionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GroupElectionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
